# Desires

Use this file to record missing context, tools, scripts, documentation, or
capabilities that would make future work easier, faster, or safer.

- `package.json` defines `bun run lint`, but ESLint is not installed in the
  project. Future work would be safer if the lint dependency/configuration were
  added or the script were removed until it is supported.
